module Data.Int;

import Stdlib.Prelude open;

gcd : Int → Int → Int
  | a b :=
    let
      terminating
      gcd' : Int -> Int -> Int
        | a b := if (a == ofNat 0) b (gcd' (mod b a) a);
    in if (a > b) (gcd' b a) (gcd' a b);
